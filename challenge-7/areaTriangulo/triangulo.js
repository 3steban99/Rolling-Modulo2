let base = parseInt(prompt("ingrese el valor de la base del triangulo"))

let altura = parseInt(prompt("ingrese la altura del triangulo"))

let area = (base*altura)/2

console.log(area)

document.write("El Resultado es igual a: ", area)